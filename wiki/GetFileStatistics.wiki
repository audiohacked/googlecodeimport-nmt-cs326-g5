#summary Use case for getting file statistics

= Details =

Actors: Basic User, Content Server, Database

  # User or Server opens a connection with and queries statistics (number of downloads per day, month, year, or total, popularity rating, sponsored status, etc.) about a given file.
  # Database completes the requested query and serves data to user or server.
  # Connection between user/server is closed.